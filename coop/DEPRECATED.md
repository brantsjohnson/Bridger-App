# DEPRECATED

This Express + Prisma co-op portal tree is **not mounted**.

Use Nest `apps/api/src/coop/` and Expo `apps/mobile/app/coop/*` instead.

Do not add routes here. See `guide-docs/complete/COOP-PORTAL.md`.
