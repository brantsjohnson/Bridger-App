// ============================================
// WHAT THIS FILE DOES (plain English):
// New route name for the collage camera (`/collage/capture`). The old
// `/story/capture` path still works and opens the same screen.
// ============================================
export { default } from '../story/capture';
